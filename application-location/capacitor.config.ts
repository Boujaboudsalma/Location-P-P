import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Application - Location',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
